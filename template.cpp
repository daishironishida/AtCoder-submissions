#include <bits/stdc++.h>

using namespace std;

int main() {
    int a;
    cin >> a;

    int N;
    cin >> N;
    vector<int> b(N);

    for (int i = 0; i < N; i++) {
        cin >> b.at(i);
    }

}